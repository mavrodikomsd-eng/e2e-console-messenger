# 📜 История версий

## v1.6.0 (GUI, Docker, админ-панель)

### 🖥️ Графический GUI (Rust + egui)
- 🎨 **Telegram Desktop-style интерфейс** — тёмная тема, пузыри сообщений, аватары.
- 😀 **Реакции** на сообщения — ПКМ → выбор emoji (👍❤️😂😮😢🔥) с анимацией.
- ✍️ **Markdown** в сообщениях — `**жирный**`, `` `код` ``, моноширинный шрифт.
- 📅 **Группировка по датам** — разделители «Сегодня», «Вчера», дата.
- 🔍 **Поиск** по сообщениям и контактам в реальном времени.
- ✓✓ **Индикаторы прочтения** — ✓ отправлено, ✓✓ доставлено (зелёная).
- 🔔 **Windows toast-уведомления** — PowerShell-интеграция, всплывают при новом сообщении.
- 📌 **Трей-иконка** — `tray-icon` crate, работает в фоне (если есть `icon.ico`).
- 📥 **Экспорт чата** — сохраняет .txt в Downloads, открывает в Проводнике.
- ⚡ **Быстрый вход** — сохранённые аккаунты (до 10), клик = вход без пароля.
- 👥 **Личные чаты** в сайдбаре — всегда видны, с бейджами непрочитанных.
- 📊 **Сортировка по активности** — кто последний писал, тот сверху.
- 🎯 **Юзернейм без `@`** — ввод `misha` → автоматически `@misha`.
- 📦 **Release**: `mesh_gui.exe` — 5.2 MB, LTO-оптимизация.

### 🐹 Сервер v2 (Go + SQLite)
- 🗄️ **SQLite** (modernc.org/sqlite, чистый Go, без CGO) — аккаунты и история.
- 📜 **Команда `/history`** — показывает последние N сообщений из БД.
- 📊 **Команда `/serverstats`** — количество аккаунтов, сообщений, размер БД.
- 💾 **Автосохранение** — все сообщения и аккаунты пишутся в SQLite.
- 🔄 **Миграция** — автоматический перенос аккаунтов из JSON в SQLite при первом запуске.

### 🐳 Docker и деплой
- 🐳 **`Dockerfile`** — multi-stage сборка Go-сервера (golang:1.22 → alpine:3.20).
- 🐳 **`Dockerfile.admin`** — Python 3.12-slim + FastAPI.
- 🐳 **`docker-compose.yml`** — два сервиса: mesh-server (1301) + mesh-admin (8080).
- 📦 **`.dockerignore`** — исключает .git, target, tests, keys.

### 🌐 Админ-панель (FastAPI)
- 🔐 **Логин** — защита паролем.
- 📊 **Дашборд** — количество пользователей, комнат, сообщений.
- ⚡ **Управление** — бан/разбан/удаление пользователей.
- 📡 **API** — `/api/stats` для внешней интеграции.
- 🎨 **Тёмная тема** — совпадает с GUI.

### 📄 Лендинг
- 📄 **`landing/index.html`** — одностраничный product-page.
- ✨ Описание возможностей, Docker-инструкции, pricing-секция.

### 🔐 Безопасность и надёжность
- 🔐 Пароли — **argon2id** (обновлено с SHA-256).
- 🏠 Приватные комнаты — пароль хэшируется, миграция автоматическая.
- 🔑 Версия протокола кадром `V` — все клиенты шлют, серверы принимают.
- 🚫 Валидация имён: нет `; : \x00 \n \r`, длина ≤ 32.
- 📏 Лимит кадра 1 МБ — нет OOM на битых данных.

### 🔧 Исправления
- ✅ Исправлена доставка сообщений после входа — клиенты повторно регистрируют ключ.
- ✅ Единый `secret.key` — клиенты ищут ключ и в текущей папке, и в корне проекта.
- ✅ `/about` показывает актуальную версию (1.6.0).
- ✅ Ротация логов: `messages.log` не растёт бесконечно.

### 📦 Что обновлено с v1.5
- Добавлен `rust_gui/` — полноценный GUI-клиент.
- Добавлен `server_v2/` — сервер с SQLite.
- Добавлен `admin/` — FastAPI админ-панель.
- Добавлен `landing/` — product-page.
- Добавлен `docker-compose.yml` — оркестрация.
- `README.md` обновлён до v1.6.0.
- `ai_agent/` — обновлены PROGRESS.md и TASKS.md.

## v1.5 (безопасность, надёжность и качество)

### 🔐 Безопасность
- 🛡️ **TOFU-защита от MITM во всех клиентах** (Python, Go, Rust): отпечатки публичных ключей
  пиров (SHA-256) запоминаются при первой встрече (`tofu.json`). При смене ключа — явное
  предупреждение; `/fingerprints` — просмотр, `/trust <ник>` — подтверждение нового ключа.
- 🚦 **Анти-флуд на серверах**: не более 30 кадров за 5 секунд
  (`config.json → server.rate_limit`), при превышении клиент отключается.
- ⏱️ **Таймауты чтения**: зависший/медленный клиент больше не держит поток сервера вечно
  (Python: `server.idle_timeout` = 600 с; Go: read deadline 10 мин).

### 🧪 Тесты
- ✅ **30 автотестов**, включая **интеграционные**: реальный сервер поднимается в тестах,
  клиенты проходят полный сценарий через TCP (регистрация → обмен E2E-сообщением → анти-флуд).
  Юнит-покрытие: AES-GCM (round-trip/подмена/AAD), X25519 ECDH между пирами, argon2id,
  фрейминг протокола, валидация имён, TOFU. Go: `server_test.go`.
  Запуск: `python -m unittest discover tests` и `go test ./...`.

### 🌐 Веб и структура
- 📊 **Веб-дашборд вместо фейкового веб-чата**: старый `web/` (Flask-эхо без шифрования)
  заменён на честный дашборд мониторинга на stdlib — подключается к серверу как клиент и
  показывает онлайн/комнаты. Содержимое сообщений недоступно по дизайну (E2E).
  Запуск: `python web/server_web.py`.
- ⚙️ **Единый конфиг для Go-сервера**: читается `config.json` (fallback на legacy `configs.json`).
- 🗑️ **Удалён Zig-клиент**: полузаготовка без сетевой части; история сохранена в git.

### 🔧 Исправления
- `/about` показывает актуальную версию проекта.
- Ротация логов: `messages.log` не растёт бесконечно (>1 МБ → `.1`); логируются только
  метаданные, без содержимого сообщений.
- Исправлен vet-варнинг Go (`fmt.Println` с лишним `\n`).

## v1.4.1 (багфиксы, готово к релизу)
- 🔧 **Исправлена доставка сообщений после входа**: клиенты повторно регистрируют публичный ключ
  после `/login`/`/register`, сервер раздаёт обновлённый ключ соседям — сообщения снова доходят,
  исчезли «невалидная команда»/тишина при отправке.
- 🗣️ **Понятная авторизация при подключении**: клиенты сразу показывают подсказку `/login`/`/register`,
  сообщения и файлы до входа получают внятный ответ (вместо игнорирования «вхолостую»),
  незнакомые/нерасшифрованные команды — явная ошибка с подсказкой.
- 🔑 **Единый общий `secret.key`**: Go- и Rust-клиенты ищут ключ и в текущей папке, и в корне проекта
  (`../secret.key`), поэтому запуск клиента из подпапки больше не создаёт «свой» ключ,
  не совпадающий с серверным.
- ✅ Сборки проверены: `go build server.go`, `cd go_client && go build ./...`, `cd rust_client && cargo build`.

## v1.3 (готово к релизу)
- 🔐 **Аккаунты с паролями**: `/register ник пароль пароль` и `/login ник пароль`.
  Без входа заблокировано всё, кроме `/register` и `/login`. Хранилище `accounts.json`
  рядом с сервером, пароли — `sha256(salt + пароль)` (голый пароль сервер не хранит).
- 🏠 **`/join` больше не создаёт комнаты** — только вход; создаёт `/createroom <имя> [пароль]`.
- 🌍 **Хост/порт из `config.json`** во всех клиентах (Go и Rust читают конфиг),
  переменные окружения `MESH_HOST` / `MESH_PORT` переопределяют конфиг.
- 🛠️ Убран жёсткий абсолютный путь в `web/server_web.py` — путь к проекту считается из расположения файла.

## v1.2.0
- 🏠 Полноценные комнаты в Python-сервере (паритет с Go): `/join`, `/leave`, `/rooms`,
  скоуп `/users` и `/roommembers` по комнате, маршрутизация сообщений.
- 🛡️ Защита ников: дубликаты отклоняются, публичный ключ привязан к имени.
- 🔑 Приватные комнаты: `/createroom <имя> <пароль>`, пароль хэшируется (SHA-256).
- 🐹 Новый **Go-клиент** (CLI на stdlib).

## v1.1
- 🔐 **X25519 E2E**: своя идентичность у каждого клиента (`identity.key`),
  per-recipient шифрование (AES-256-GCM + ECDH), защита от подмены имени по публичному ключу.
- 📂 Передача **файлов** (`/file <путь>`), зашифрованных на каждого получателя.
- 🏠 Комнаты (черновая версия).
- 🦀 Новый **Rust-клиент**.

## v1.0
- Консольный мессенджер на Python, E2E AES.